{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimum capability set: IPC invoke + notification + file read/write scoped to the user-selected workspace. No generic shell execute. See PRD §14 `Tauri v2 capabilities`.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:app:default",
    "core:menu:default",
    "core:path:default",
    "notification:default",
    "dialog:allow-open"
  ]
}
